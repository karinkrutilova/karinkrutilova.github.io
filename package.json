{
  "name": "portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --check public/gallery.js && node --check public/admin/bulk-upload/upload.js && node --check public/admin/order.js && node scripts/check-bulk-upload.mjs && node scripts/check-order.mjs && node scripts/check-crops.mjs && node scripts/check-build.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "allowScripts": {
    "esbuild": true
  }
}

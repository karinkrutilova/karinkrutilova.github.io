// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://magicsk.github.io',
  base: '/portfolio',
  trailingSlash: 'always',
});
